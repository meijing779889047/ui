<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.coordinatortablayout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.UIActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MeterialDesignActivity" />
        <activity
            android:name=".ui.toolbar.TransparentBarActivity"
            android:theme="@style/TransparentBarStyle" />
        <activity
            android:name=".ui.toolbar.ToolBarAttributeActivity"
            android:theme="@style/TransparentBarStyle" />
        <activity
            android:name=".ui.toolbar.CustomToolbarActivity"
            android:theme="@style/CustomTheme" />
        <activity
            android:name=".ui.coordinatorlayout.CoordinatorLayoutActivity"
            android:theme="@style/TransparentBarStyle" />
        <activity
            android:name=".ui.inputlayout.LoginActivity"
            android:theme="@style/TransparentBarStyle" />
        <activity
            android:name=".ui.floatactionbutton.FloatActionButtonActivity"
            android:theme="@style/TransparentBarStyle" />
        <activity
            android:name=".ui.cardview.CardViewActivity"
            android:theme="@style/TransparentBarStyle" />
        <activity android:name=".ui.toolbar.CollapsingToolBarLayoutActivity"
            android:theme="@style/TransparentBarStyle" ></activity>
    </application>

</manifest>